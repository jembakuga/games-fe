import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const PlayerPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default PlayerPageLayout;