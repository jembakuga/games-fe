import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const CommissionPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default CommissionPageLayout;